{
  "name": "todo-list-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm --filter web test",
    "ctx:sync": "tsx scripts/sync.ts",
    "ctx:sync:local": "tsx scripts/sync.ts local",
    "ctx:sync:global": "tsx scripts/sync.ts global",
    "ctx:validate": "tsx scripts/validate.ts",
    "ctx:validate:local": "tsx scripts/validate.ts local",
    "ctx:validate:global": "tsx scripts/validate.ts global"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.5.2",
    "glob": "^11.0.3",
    "js-yaml": "^4.1.0",
    "tsx": "^4.20.6"
  }
}